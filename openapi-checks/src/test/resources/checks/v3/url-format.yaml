openapi: 3.0.0
info:
  title: Title
  description: Title
  version: 1.0.0
  termsOfService: randomString # Noncompliant {{Make sure to only use an valid URL.}}
  contact:
    url: randomString # Noncompliant {{Make sure to only use an valid URL.}}
  license:
    name: someName
    url: randomString # Noncompliant {{Make sure to only use an valid URL.}}
paths: {}
